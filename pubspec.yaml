name: study_japanese
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  sqflite: ^2.4.2
  cloud_firestore: ^5.6.6 # 添加 Firebase Firestore 套件
  firebase_core: ^3.13.0 # 添加 Firebase 核心套件
  http: ^1.0.0
  html: ^0.15.0
  webview_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/image
